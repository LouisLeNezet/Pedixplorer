# Create the user interface for the ped_shiny application

Create the user interface for the ped_shiny application

## Value

[`shiny::shinyUI()`](https://rdrr.io/pkg/shiny/man/shinyUI.html)

## Examples

``` r
if (interactive()) {
    ped_shiny()
}
```
